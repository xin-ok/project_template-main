@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
# find_dependency(...)

include("${CMAKE_CURRENT_LIST_DIR}/@export_name@Targets.cmake")
check_required_components(@export_name@)

if(NOT @export_name@_FIND_QUIETLY)
    message(STATUS "Found @export_name@ ${@export_name@_VERSION} (${@export_name@_DIR})")
endif()